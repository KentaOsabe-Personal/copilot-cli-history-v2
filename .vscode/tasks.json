{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "backend: debug server",
      "type": "shell",
      "command": "docker compose -f docker-compose.yml -f docker-compose.debug.yml up --build -d backend",
      "problemMatcher": []
    }
  ]
}
